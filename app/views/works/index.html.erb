<div class="movies">
  <h2>Top Movies</h2>
  <% @works.where(category: 'movie').each do |work|%>
    <p><strong>Title:</strong> <%= link_to work.title, work_path(work.id)%>, <strong>Category:</strong> <%=work.category%></p>
  <%end%>
</div>


<div class="books">
  <h2>Top Books</h2>
  <% @works.where(category: 'book').each do |work|%>
    <p><strong>Title:</strong> <%= link_to work.title, work_path(work.id)%>, <strong>Category:</strong> <%=work.category%></p>
  <%end%>
</div>

<div class="albums">
  <h2>Top Albums</h2>
  <% @works.where(category: 'album').each do |work|%>
    <p><strong>Title:</strong> <%= link_to work.title, work_path(work.id)%>, <strong>Category:</strong> <%=work.category%></p>
  <%end%>
</div>

